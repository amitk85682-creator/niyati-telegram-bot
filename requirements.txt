# Required system packages first
setuptools>=70.0.0
wheel>=0.41.2
pip>=25.1.1

# Core libraries
google-generativeai==0.3.0
elevenlabs==0.2.24
supabase==2.0.0
fastapi==0.104.1
uvicorn==0.24.0
websockets==12.0
python-dotenv==1.0.0
nltk==3.8.1
textblob==0.17.1

# Numpy fix: latest wheel (compatible with Python 3.13)
numpy==2.1.1

git add requirements.txt
git commit -m "fix: use numpy 2.1.1 and updated setuptools"
git push
